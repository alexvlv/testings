. ${ALIAS_DIR}/terminal.sh

alias ya.sr='(tt YAND; yandex-disk start --read-only && sleep 1 && watch yandex-disk status)'
alias ya.s='(tt YAND; yandex-disk start && sleep 1 && watch yandex-disk status)'
alias ya.o='(yandex-disk stop)'
alias ya.w='(tt YAND; watch yandex-disk status)'
